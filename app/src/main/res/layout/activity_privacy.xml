<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:background="@color/light_grey_e1"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/viewPagerPrivacy"
        android:layout_above="@+id/btnActionPrivacy"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnActionPrivacy"
        android:text="@string/privacy_activity_setup_configuration_btn_title"
        android:fontFamily="@font/stolzl_medium"
        android:textSize="12sp"
        android:textColor="@color/white"
        android:background="@drawable/bg_gradient_button"
        app:backgroundTint="@null"
        android:layout_marginHorizontal="16dp"
        android:layout_marginVertical="32dp"
        android:layout_alignParentBottom="true"
        android:lines="1"
        android:maxLines="1"
        style="@style/Widget.Material3.Button.ElevatedButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

</RelativeLayout>